# Bobby × VIGOR 教程页｜本地验收记录

2026-09-23｜Owner：工程产品 Agent｜页面执行：Luna｜范围：拆页版本的本地更新；CJ 已授权本教程素材随课程页面进入公开 GitHub 仓库。公开发布状态见文末。

## 已完成、已验证

- 页面为静态中文教程，包含 **2 条**可播放影片、**3 组**图片对照、5 步流程与检查表；首屏时间线区分 87.583 秒无牌旧版、30.05 秒 VIGOR 原片和 25.458 秒 V2。
- Chrome 本地浏览器实际打开页面：桌面首屏、对照区和 390px 手机布局可读；手机无横向溢出。主规格对照图可放大，按 Esc 可关闭。
- 原片与 V2 均在页面手动启动播放；第二条启动时第一条暂停。两条视频保持竖屏 9:16，没有自动播放。
- HTML 的 `src`、`href`、`data-image` 共 48 处引用逐一检查，本地缺失数 **0**。初次浏览发现的 `规格纠错前后.webp` 缺失已补齐，再次刷新后显示正常。
- `node --check script.js` 与 `node --check preview-server.mjs` 通过；浏览器错误日志为空。预览服务仅监听 `127.0.0.1:4173`，页面 HEAD 返回 200 与 `X-Robots-Tag: noindex, nofollow`；V2 视频 Range 请求返回 206。
- 素材从项目文件复制到页面目录，原件位置不变；未向外部服务上传或发布。

## 事实与使用边界

- VIGOR 的 183 × 61 × 1.5 cm 和黑、紫、粉、蓝四类颜色为 **2026-09-22 项目核对记录**；本轮官方页抓取 403、Shopee 短链抓取不可访问，因此未复核实时库存、价格和下单 SKU。
- 页面展示九镜头提示词的项目规划文件，同时说明原片的具体生成任务和逐字提示词**未存证**；不把规划文件冒充生成原片的证明。
- Bobby 173 cm 为角色设定；两端各约 5 cm 是居中摆放的理论余量。V2 的规格卡纠正画面文字，不表示视频中垫子的几何尺寸已实测或重建。
- Bobby、Alex 仍为 draft 候选；CJ 已授权其在本课程页面公开展示，未授权 Canonical、跨项目、开放素材重用、训练/克隆或一般商业使用。品牌图也只在本教程示例范围展示；商品短链存在于影片文字，不等于平台已挂商品卡。
- 本次确认浏览器能启动两条影片，**没有做完整的人工有声连续观看/试听**，因此不把声音听感或整片观看体验列为已验收。

本地预览按 [README.md](README.md) 启动；素材出处见 [ASSET_SOURCES.md](ASSET_SOURCES.md)。

## 2026-09-23 补充验收：产品参考图教学

- 第 02 步新增实物参考缺口说明和四种补拍清单：整垫俯拍、Logo 近拍、斜侧光纹理微距、垫边与绑带细节；只要求标注已量过的位置与尺寸，不虚构 Logo 距离或纹理深度。
- 产品对照 B 和图片说明已把品牌页展示图、生成三视图区分为不同证据等级。新增的 V2 纹理近景有明确「生成画面，非实物纹理证明」标注；缺少纹理微距参考仅表述为画面偏平的可能原因之一。
- 在本机浏览器实际检查 1047px 桌面与 390px 手机视窗，新增卡片、清单和图片可读；临时手机视窗检查后已恢复默认尺寸。新增近景在放大弹层可显示。
- HTML 的 `src`、`href`、`data-image` 共 50 处本地引用逐一检查，缺失数 **0**；页面与新增图片的本地 HTTP HEAD 都返回 **200**。新增图片与 V2 原始抽帧的 SHA-256 相同；`node --check` 检查两个 JS 文件均通过。

## 2026-09-23 课程结构更新验收

- 页面导航已显示「第零章：人物参考图」「第一章：商品短片」。第一章入口位于第零章之后，原有 Bobby × VIGOR 两条影片、规格与参考图对照、五步流程、检查表仍保留。
- 第零章展示 Alex 六格与三栏归档旧图；浏览器实际显示两图，布局说明与图对应。Alex 独立于 Bobby，保持 `CAND-FIT-ALEX-001` draft；人物图标明 AI 示例来源，不宣称真实照片。六格旧版背面示例没有头，另注明提示词模板允许新生成的背面保留头发。
- 浏览器视觉检查：桌面章节首屏与两张示例图区域；Bobby 旧六格明确标为本案曾使用的 draft 候选，入口注明未按新版 Skill 重做；三栏显示正面服装、背面服装、大脸；旧版三栏示例背面实际无头，caption 注明未来可按需留发。六格旧版示例上排三格均裁头，下排显示大脸。另以 390px 手机视口实际打开第零章，文字、示例卡和提示词区可读，页面 `scrollWidth` 与 `clientWidth` 同为 375px，无横向溢出；临时视口检查后已恢复默认尺寸。
- 三个提示词折叠卡已出现，两视图复制按钮在浏览器显示「已复制」。提示词与人物参考图 Skill 的新版模板同步：角色 ID、outfit_id、输入参考、目标像素核对，正面/侧面无头、背面可留头发但不得挡服装、六格底部三张大脸。
- 检查 `index.html` 本地媒体与 Markdown 引用共 39 次、21 个唯一媒体路径（目录内媒体引用检查），缺失 0；`node --check script.js` 与 `preview-server.mjs` 通过。两张 Alex PNG 的源哈希与归档文件核对一致：六格 `6FCEB487…821D6253E0`，三栏 `C42024C2…D3A7442`。清晰原 PNG 为页面示例放大保留，分别约 1.9 MB 与 16.1 MB。
- `角色资产_manifest.md` 记录 Alex 两张图及 Bobby 主参考的母库路径、ID、状态、哈希、复制日期、用途、GitHub 展示边界及未核实的生成平台/商业条款；未添加开放素材许可证。
- 公开范围限本教程页面示例；不等于素材开放再利用、Canonical 或一般商业授权。网站部署、完整人工观看/试听和外部平台挂链状态仍未验证。


## 2026-09-23 三页课程目录与章节拆分（本地结构检查）

- 根页面改为课程目录，逐项介绍免费第零章（人物参考图资产）与第一章（Bobby × VIGOR 商品短片），目录和页首均可直接进入第一章。新增独立 chapter-0.html 与 chapter-1.html；第一章以「画面很好看，为什么还不能直接卖产品？」开场，不要求先修第零章。
- 第零章保留两视图、三栏、六格说明、Alex 例图、三种复制提示词、资产登记、图片放大与来源/状态说明。第一章保留两条影片、所有原图对照、五步流程、资源和证据边界。
- 三页共用原有 CSS 与交互脚本；目录与章节导航相互链接。HTML 本身不带 noindex，以适应公开课程；仅本地预览服务继续返回 X-Robots-Tag: noindex, nofollow，并绑定 127.0.0.1。
- 本地静态检查：三页引用分别为 16、13、41 个，缺失资源均为 0；页面含 0、0、2 条视频；第零章提示词复制控件、第一章五步和两视频均存在；node --check script.js 与 node --check preview-server.mjs 通过。
- 本地 HTTP 检查：/、三页路径均返回 200 和 noindex 响应头；越界路径返回 404。检查后已关闭预览服务器。
- 当时尚未完成：浏览器视觉/键盘验收；后续复核结果见下一节。此阶段没有操作 GitHub、DNS 或外部发布。

## 2026-09-23 三页实际浏览器复核

- 在本地 HTTP 预览的桌面和 390px 手机视口检查课程目录、第零章和第一章；三页章节导航可跳转。课程目录主标题在手机上分成两行，没有孤立的单字。
- 第零章两视图提示词复制后显示「已复制」；Alex 三栏图片可放大，并可用 Esc 关闭弹层。
- 第一章两条影片在 Chrome 中手动播放，读取到影片已加载且无媒体错误；原始生成片播放到约 30 秒，V2 播放到约 16 秒。Codex 内置预览器点击原片时预览标签页崩溃，因此影片播放以 Chrome 的实际检查为准，不把内置预览器的崩溃写成网站正常运行的证明。
- 这次仍未完整有声观看两条影片，故不声称已验收整片声音和观看体验。
- 本地复核当时尚未添加 GitHub remote；已准备 `learn.cj-chan.work` CNAME 文件。后续的公开发布检查见下一节。

## 2026-09-23 GitHub Pages 与域名发布检查

- 课程已推送至公开仓库 `CJchan09/cj-digital-human-product-video-course`；初始远端 `main` 与本地提交均为 `9478d71ee081cc0ea299a03192d4f735c7413fc7`。远端两条 MP4 和两张 Alex PNG 存在，大小非零。
- GitHub Pages 从 `main` 根目录构建成功，自定义域名登记为 `learn.cj-chan.work`。Cloudflare 已添加 `learn` CNAME → `cjchan09.github.io`，设为仅 DNS；公开解析返回该目标。
- HTTP 上的课程目录、第零章、第一章及两条 MP4 均返回 200，视频 Content-Length 与远端文件大小一致。此项只证明部署和文件可访问，不是 HTTPS 验收。
- 早期 HTTPS 因证书尚未签发而失败；2026-09-23 随后 GitHub Pages 证书状态变为 `approved`，覆盖 `learn.cj-chan.work`，已启用 `https_enforced=true`。严格 TLS 校验下，HTTPS 首页、第零章和第一章均返回 200；HTTP 首页返回 301，`Location` 为 `https://learn.cj-chan.work/`。
- 两条 MP4 在 HTTPS 下均返回 200、`video/mp4`，`Content-Length` 分别为 31,213,891 与 9,617,687 字节，和仓库文件大小一致。Chrome 实际打开 HTTPS 首页，导航、主图与样式正常；页面源码未发现 `http://` 混合内容引用。
- 可分享入口：[https://learn.cj-chan.work/](https://learn.cj-chan.work/)；完整人工试听与商品当前销售状态仍未验证，相关边界见上文。

## 2026-09-23 补入早期无牌瑜伽展示片

- CJ 指出课程已提到 87.58 秒早期无牌版本，影片区却没有放进完整视频。已从 `../../Bobby_NBR瑜伽垫_20260919/剪辑成品_v1/Bobby_瑜伽垫_剪辑说明字_v1.mp4` 复制完整成片到 `assets/00_无牌瑜伽展示版.mp4`，原件保持不动；源与页面副本 SHA-256 同为 `EF75CAA5AC49601477BA8DF3080F659488713EE2B5A9D868993DCBAFF88C8011`，大小 16,751,568 字节。该片原项目技术检查记录为 87.583333 秒、720 × 1280、24 fps、2102 帧、H.264 + AAC，完整解码无错。
- 第一章影片区现按 A 无牌瑜伽展示旧案、B VIGOR 原始生成片、C VIGOR V2 排列；标题和说明明确 A 与 B/C 是不同 SKU。无牌片并非完全没有商品信息，它有厚度、长度、躺姿和收纳片段，但动作展示占主导；不把片长差异表述成完播或销售数据。
- 本地预览对新 MP4 返回 200、`video/mp4`、16,751,568 字节。Chrome 实测旧片可手动播放至约 13 秒，媒体 `duration=87.583333`、`readyState=4`、无媒体错误；三张封面和三列排版可见。390px 手机视口改为单列，页面 `scrollWidth=375`，未发生横向溢出。两条 VIGOR 视频保留，仍是手动播放且 JS 只允许一条同时播放。
- 视频吸引力检查：输入为旧案完整文件、既有导出技术记录、两张取样检查板与 Chrome 前 13 秒；未完整有声连续观看。作为直接卖货片的故事结构判定为 **REVISE**：开场主要展示瑜伽动作，产品信息较晚且分散；作为课程中的对比证据可用。不能据此推断真实观众留存、商品效果或销量。
- 线上发布：GitHub Pages 在 2026-09-23 12:07:38 UTC 完成本轮构建，Pages API 返回 `status=built`、`https_enforced=true`、构建错误为空。严格 HTTPS 下第一章返回 200，页面显示 A 无牌旧案 → B VIGOR 生成片 → C VIGOR V2；新增 MP4 的 HEAD 返回 200、`video/mp4`、16,751,568 字节，支持 byte ranges。Chrome 实际打开线上 `https://learn.cj-chan.work/chapter-1.html#films`，手动播放旧片到约 7 秒，画面和进度正常后暂停。完整有声连续试听仍未完成。
